#!/usr/bin/env ansible-playbook
---
- name: Apply the singlenode role to the singlenode nodes
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  roles:
  # pxh: change this when other NR is merged XXX
    - ska_collections.minikube.setup
    - singlenode
  tags:
    - singlenode
