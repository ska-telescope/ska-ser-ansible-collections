---

- name: Destroy Binderhub
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  gather_facts: true

  tasks:
    - name: Destroy Binderhub
      ansible.builtin.import_role:
        name: binderhub
        tasks_from: destroy
