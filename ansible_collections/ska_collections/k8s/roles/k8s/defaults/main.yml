---
kubernetes_ubuntu_version: xenial
kubernetes_version: 1.25.5
kube_version: "{{ kubernetes_version }}-00"

k8s_deb_repo_key_url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
k8s_key_file: /usr/share/keyrings/k8s.key
k8s_key_file_gpg: "{{ k8s_key_file }}.gpg"
k8s_deb_repo_url: "deb [signed-by={{ k8s_key_file_gpg }}] http://apt.kubernetes.io/ kubernetes-{{ kubernetes_ubuntu_version }} main"


# k8s_interface: ens.*,eth.*

# k8s_dns_entry: name.of.lb

