dns_advertise_domain: "{{ _ | mandatory('dns_advertise_domain definition is mandatory') }}"
dns_server_addresses: []
# Backwards compatibility
dns_k8s_clusters:
  - domain: svc.cluster.local
    address: "{{ svc_cluster_local_dns_address | default('10.200.10.1') }}"
