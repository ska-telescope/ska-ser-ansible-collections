apiVersion: v1
kind: Service
metadata:
  name: minio-tenant
  namespace: {{ gitlab_runner_minio_namespace }}
spec:
  type: LoadBalancer
  selector:
    v1.min.io/tenant: minio
  ports:
  - name: http-minio
    port: 9000
    protocol: TCP
    targetPort: 9000
    nodePort: 30900