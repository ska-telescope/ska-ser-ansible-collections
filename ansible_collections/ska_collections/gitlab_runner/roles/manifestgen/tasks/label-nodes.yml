- name: Add label to nodes
  kubernetes.core.k8s:
    state: patched
    kind: Node
    name: {{ item }}
    definition:
      metadata:
        labels:
          {{ gitlab_runner_manifestgen_node_label }}: true
  with_items: "{{ gitlab_runner_manifestgen_nodes }}"
