---

- name: Delete metallb Helm release
  kubernetes.core.helm:
    state: absent
    name: "metallb"

- name: Delete cordns Helm release
  kubernetes.core.helm:
    state: absent
    name: "extdns"
