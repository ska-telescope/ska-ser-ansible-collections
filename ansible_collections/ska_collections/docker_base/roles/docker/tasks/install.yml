---
- name: Allow updating Docker apt packages
  ansible.builtin.dpkg_selections:
    name: "{{ item }}"
    selection: install
  when: docker_allow_upgrade

- name: Install docker CE (apt)
  ansible.builtin.apt:
    name: "{{ item }}={{ docker_deb_version }}"
    state: present
    allow_downgrade: true
  notify:
    - Restart docker

- name: Hold Docker apt packages
  ansible.builtin.dpkg_selections:
    name: "{{ item }}"
    selection: hold
