---
- name: Check workload cluster
  ansible.builtin.debug:
    var: capi_cluster

- name: Check kubeconfig
  ansible.builtin.debug:
    var: ansible_env.KUBECONFIG

# TODO: Destroy the cluster object