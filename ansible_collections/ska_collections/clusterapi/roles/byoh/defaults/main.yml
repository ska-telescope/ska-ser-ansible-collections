---
debug: false

byoh_current_dir: .
byoh_project_dir: ~/git/public/cluster-api-provider-bringyourownhost
byoh_local_agent_file: "{{ byoh_project_dir }}/bin/byoh-hostagent-linux-arm64"
byoh_remote_agent_file: /usr/local/bin/byoh-hostagent-linux-arm64
byoh_local_kubeconfig_file: "{{ byoh_current_dir }}/bootstrap-kubeconfig.conf"
byoh_remote_kubeconfig_file: /etc/k8s-bootstrap-kubeconfig.conf
byoh_kube_namespace: default
byoh_agent_version: v0.3.1
byoh_agent_url: https://github.com/vmware-tanzu/cluster-api-provider-bringyourownhost/releases/download

byoh_kubernetes_ubuntu_version: xenial
byoh_kubernetes_version: 1.25.5
byoh_kube_version: "{{ byoh_kubernetes_version }}-00"

byoh_k8s_deb_repo_key_url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
byoh_k8s_key_file: /usr/share/keyrings/k8s.key
byoh_k8s_key_file_gpg: "{{ byoh_k8s_key_file }}.gpg"
byoh_k8s_deb_repo_url: "deb [signed-by={{ byoh_k8s_key_file_gpg }}] http://apt.kubernetes.io/ kubernetes-{{ byoh_kubernetes_ubuntu_version }} main"

# sudo curl -fsSLo /usr/share/keyrings/k8s.key https://packages.cloud.google.com/apt/doc/apt-key.gpg

# https://packages.cloud.google.com/apt kubernetes-xenial
# B53DC80D13EDEF05
# https://keyserver.ubuntu.com/pks/lookup?op=get&search=0xa362b822f6dedc652817ea46b53dc80d13edef05
