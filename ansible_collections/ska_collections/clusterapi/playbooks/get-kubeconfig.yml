#!/usr/bin/env ansible-playbook
---

- name: "Apply the get workload kubeconfig from management cluster role to nodes"
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  roles:
    - kubeconfig
