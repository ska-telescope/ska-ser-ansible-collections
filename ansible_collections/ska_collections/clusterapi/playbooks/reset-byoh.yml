#!/usr/bin/env ansible-playbook
---
- name: Apply the cleanbyohost role to controlplane and worker nodes
  hosts: "{{ target_hosts }}"
  become: true
  vars:
    target_hosts: localhost
  roles:
    - cleanbyohost
