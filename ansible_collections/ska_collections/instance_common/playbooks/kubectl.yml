#!/usr/bin/env ansible-playbook
---
- name: Install kubectl
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  tasks:
    - name: Install kubectl
      ansible.builtin.include_role:
        name: kubectl
