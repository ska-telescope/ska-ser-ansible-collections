---
- name: Ingress Proxy
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  gather_facts: true
  tasks:
    - name: create proxy server
      include_role:
        name: reverseproxy
