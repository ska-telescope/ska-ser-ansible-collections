---

- name: Deploy snmp_exporter
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  tasks:
    - name: Deploy snmp_exporter
      ansible.builtin.import_role:
        name: snmp
        tasks_from: snmp_exporter
