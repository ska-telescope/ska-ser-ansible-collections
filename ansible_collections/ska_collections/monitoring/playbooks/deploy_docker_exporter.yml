---
- name: Deploy docker exporter
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
    mode: "docker"
  become: true
  become_method: sudo
  become_flags: '-S -n'
  roles:
    - prometheus
