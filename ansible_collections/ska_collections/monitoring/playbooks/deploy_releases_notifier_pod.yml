---

- name: Deploy Releases Notifier chart
  vars:
    target_hosts: localhost
  become: true
  gather_facts: false

  tasks:
    - name: Deploy Releases Notifier chart
      ansible.builtin.import_role:
        name: releases_notifier
        tasks_from: deploy_pod