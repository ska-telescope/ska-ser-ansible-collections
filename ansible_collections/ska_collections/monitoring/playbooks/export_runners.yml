---
- name: Export gitlab runners to prometheus
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
    mode: "runner"
  become: true
  become_method: sudo
  become_flags: '-E -S -n'
  roles:
    - prometheus
