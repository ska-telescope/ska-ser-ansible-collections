---
- name: "Destroy {{ elasticsearch_exporter_name }} container"
  community.general.docker_container:
    name: "{{ elasticsearch_exporter_name }}"
    state: absent
    stop_timeout: 60
    timeout: 120
    force_kill: false
