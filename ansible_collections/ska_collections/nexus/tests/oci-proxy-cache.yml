- name: Test Nexus OCI Proxy-Cache
  hosts: "{{ target_hosts }}"
  vars:
    target_hosts: localhost
  become: true
  tasks:
    - name: Test Nexus OCI Proxy-Cache
      ansible.builtin.include_role:
        name: ska_collections.nexus.common
        tasks_from: ../tests/oci-proxy-cache
